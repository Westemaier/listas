algoritmo "SomaParametros"
var
   x, y: inteiro
procedimento soma (var a , b: inteiro)
inicio
   A <- A + 1
   B <- B + 2
   escreval("Valor de A = ", a)
   escreval("Valor de B = ", b)
   escreval("Soma A + B = ", a+b)
   fimprocedimento
inicio
   x <- 4
   y <- 8
   soma (x, y)
   escreval("Valor de X = ", x)
   escreval("Valor de Y = ", Y)
fimalgoritmo
